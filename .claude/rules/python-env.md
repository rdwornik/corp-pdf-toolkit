# Python Environment — corp-pdf-toolkit

- Python >=3.10, target 3.12
- Virtual env: .venv\Scripts\Activate.ps1
- Install: pip install -e .
- Flat layout (scripts at root, no src/ directory)
- Uses argparse CLI (standalone scripts)
- All processing local via Ollama — no cloud API keys needed
